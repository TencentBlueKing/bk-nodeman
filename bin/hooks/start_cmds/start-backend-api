#!/bin/bash
gunicorn --timeout 300 -w 8 -b "[::]:${BACKEND_API_PORT}" wsgi:application --access-logfile - --error-logfile - --max-requests=10000 --max-requests-jitter 1000 -k gthread --threads 5 --access-logformat '[%(h)s] %({request_id}i)s %(u)s %(t)s "%(r)s" %(s)s %(D)s %(b)s "%(f)s" "%(a)s"'
