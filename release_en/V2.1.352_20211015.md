## 2.1.352 Release Notes

### Optimized
- DB reconnection mechanism (close #211)
- Upgrade script virtualenv path derives from global vars (closed #207)

### Fixed
- Strategy inspection error after host removal (fixed #206)
- Windows agent pluginipc should be 47200 (fixed #210)

### Added
- pre-commit auto generation of dev_log (closed #195)
- Abort install flow if fetching config file fails (close #195)
